using System;
using System.IO;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Proyecto_Final___
{
    class Program
    {
        //fin del menu 


        class Alumno
        {
            private string nombre;
            private string exp;
            private string plan;

            public void buscarAlumno(string expediente)
            {
                if (File.Exists("files/alumnos.txt"))
                {
                    StreamReader reader = new StreamReader("files/alumnos.txt");
                    string lee = reader.ReadLine();
                    bool a;
                    while (lee != null)
                    {
                        a = lee.StartsWith(expediente);
                        if (a)
                        {
                            Console.WriteLine(lee);
                        }
                        lee = reader.ReadLine();
                    }
                    reader.Close();
                }
                else
                {
                    Console.WriteLine("El archivo no existe.");
                }
            }
            public void insertaAlumno()
            {
                Console.WriteLine("Ingresa el expediente del alumno:");
                exp = Console.ReadLine();
                Console.WriteLine("Ingresa el nombre del alumno:");
                nombre = Console.ReadLine();
                Console.WriteLine("Ingresa el plan del alumno:");
                plan = Console.ReadLine();
                // StreamWriter escribe2 = new StreamWriter("files/alumnos2.txt");
                StreamWriter escribe = File.AppendText("files/alumnos.txt");
                escribe.WriteLine(exp + " ; " + nombre + " ; " + plan);
                escribe.Close();
                // escribe2.Close(); 
                Console.Clear();
            }
            public void leeArchivo(string archivo)
            {
                if (File.Exists(archivo))
                {
                    StreamReader reader = new StreamReader(archivo);
                    string lee = reader.ReadLine();
                    while (lee != null)
                    {
                        Console.WriteLine(lee);
                        lee = reader.ReadLine();
                    }
                    reader.Close();
                }
                else
                {
                    Console.WriteLine("El archivo no existe.");
                }
            }

            public static void Main(string[] args)
            {
                Alumno a = new Alumno();
                //a.leeArchivo("files/alumnos.txt");
                //a.insertaAlumno();
                //a.leeArchivo("files/alumnos.txt");
                Console.WriteLine("Escribe el expediente a buscar");
                a.buscarAlumno(Console.ReadLine());

                for (int i = 1; i == 1; i++)
                {
                    int opcion = 0;


                    //Console.WriteLine("")
                    Console.WriteLine("Seleccione una Opcion");
                    Console.WriteLine("1.-    Buscar por expediente");
                    Console.WriteLine("2.-    buscar por carrera");
                    Console.WriteLine("3.-    Buscar por Nombre");
                    Console.WriteLine("4.-    Buscar por Promedio");

                    opcion = int.Parse(Console.ReadLine());
                    Console.Clear();

                    Console.WriteLine();

                    switch (opcion) // JUICIO DE VALOR 
                    {
                        case 1:
                            //variables 
                            Console.WriteLine();
                            break;
                        case 2:
                            // variables 
                            Console.WriteLine();
                            break;
                        case 3:
                            //variables 
                            Console.WriteLine();
                            break;
                        case 4:
                            //variables
                            Console.WriteLine();
                            break;
                    }
                    Console.WriteLine("Escriba los datos");
                    //variabe+ int.Parse(Console.ReadLine());

                    Console.ReadLine();
                    Console.WriteLine("Desea hacer otra Opcion  1.- SI  2.-NO");
                    i = int.Parse(Console.ReadLine());
                }

            }
        }

    }


}